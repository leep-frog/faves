{
  "name": "faves",
  "displayName": "faves",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.81.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "faves.add",
        "title": "Faves: Add to favorites",
        "icon": "$(star)"
      },
      {
        "command": "faves.remove",
        "title": "Faves: Remove from favorites"
      },
      {
        "command": "faves.toggle",
        "title": "Faves: Add or remove the file from favorites"
      },
      {
        "command": "faves.search",
        "title": "Faves: Search"
      }
    ],
    "menus": {
      "editor/title/run": [
        {
          "command": "faves.add"
        }
      ],
      "editor/title/context": [
        {
          "command": "faves.add"
        }
      ]
    },
    "configuration": {
      "type": "object",
      "title": "Faves",
      "properties": {
        "faves.favorites": {
          "type": "array",
          "default": [],
          "description": "List of favorited files",
          "items": {
            "type": "object",
            "description": "Data for a specific favorited item",
            "properties": {
              "uri": {
                "type": "object",
                "description": "VSCode URI reference"
              }
            }
          }
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.81.0",
    "@types/mocha": "^10.0.1",
    "@types/node": "16.x",
    "@typescript-eslint/eslint-plugin": "^6.4.1",
    "@typescript-eslint/parser": "^6.4.1",
    "eslint": "^8.47.0",
    "glob": "^10.3.3",
    "mocha": "^10.2.0",
    "typescript": "^5.1.6",
    "@vscode/test-electron": "^2.3.4"
  }
}
